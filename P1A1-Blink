#define LED 2

void setup() {
  // put your setup code here, to run once:
pinMode(LED,OUTPUT);      //pinMode(pin_name,OUTPUT);
}
void loop() {
  // put your main code here, to run repeatedly:
digitalWrite(LED,HIGH);   //digitalWrite(pin_name,OUTPUT);
delay(500);
digitalWrite(LED,LOW);
delay(500);
}
